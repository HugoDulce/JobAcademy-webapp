---
deck: "JobAcademy::Uncategorized::3-Algorithm"
tags: [uncategorized, programming, build]
card_id: "nb-3P-01"
fire_weight: 0.9
notion_last_edited: "2026-02-08T17:05:00.000Z"
concept_node: "NB"
---

# nb-3P-01

START
Implement GaussianNB fit() from scratch in numpy. Store priors, means, variances.
END

START
python
import numpy as np

class GaussianNB:
    def fit(self, X, y):
        self.classes_ = np.unique(y)
        n_classes = len(self.classes_)
        n_features = X.shape[1]
        
        # Initialize storage
        self.priors_ = np.zeros(n_classes)
        self.means_ = np.zeros((n_classes, n_features))
        self.vars_ = np.zeros((n_classes, n_features))
        
        # Compute for each class
        for idx, c in enumerate(self.classes_):
            X_c = X[y == c]
            self.priors_[idx] = X_c.shape[0] / X.shape[0]
            self.means_[idx, :] = X_c.mean(axis=0)
            self.vars_[idx, :] = X_c.var(axis=0)
        
        return self

END
