---
deck: "JobAcademy::Uncategorized::3-Algorithm"
tags: [uncategorized, programming, build]
card_id: "nb-3P-02"
fire_weight: 0.9
notion_last_edited: "2026-02-08T17:05:00.000Z"
concept_node: "NB"
---

# nb-3P-02

START
Implement GaussianNB predict() using stored parameters. Use log probabilities.
END

START
python
def predict(self, X):
    log_probs = []
    
    for idx, c in enumerate(self.classes_):
        prior = np.log(self.priors_[idx])
        
        # Gaussian log likelihood
        var = self.vars_[idx, :]
        mean = self.means_[idx, :]
        likelihood = -0.5 * np.sum(np.log(2 * np.pi * var))
        likelihood -= 0.5 * np.sum(((X - mean) ** 2) / var, axis=1)
        
        log_prob = prior + likelihood
        log_probs.append(log_prob)
    
    log_probs = np.array(log_probs).T
    return self.classes_[np.argmax(log_probs, axis=1)]

END
